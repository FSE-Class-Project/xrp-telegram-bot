# Pre-commit hooks for XRP Telegram Bot
# Windows, Mac, and Linux

# Exclude directories that shouldn't be checked
exclude: |
  (?x)^(
    \.venv/.*|
    venv/.*|
    \.env|
    __pycache__/.*|
    \.pytest_cache/.*|
    htmlcov/.*|
    \.coverage|
    .*\.egg-info/.*|
    build/.*|
    dist/.*|
    \.git/.*|
    \.mypy_cache/.*|
    \.tox/.*|
    alembic/versions/.*\.py$|
    migrations/.*
  )$

repos:
  # MyPy - Static type checker (run FIRST to avoid conflicts)
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.8.0
    hooks:
      - id: mypy
        additional_dependencies: [types-requests, types-redis]
        args: [--config-file=pyproject.toml]

  # Ruff - Fast Python linter and formatter (run AFTER MyPy)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.9
    hooks:
      # Run the formatter first (safest)
      - id: ruff-format
      # Run the linter with fixes (more risky, so last)
      - id: ruff
        args: [--fix]  # Removed --exit-non-zero-on-fix to be less aggressive

  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: check-json
        exclude: .*/settings\.local\.json$
      - id: check-yaml
        exclude: render.yaml  # Render might have special syntax
      - id: check-toml
      - id: end-of-file-fixer
        exclude: |
          (?x)^(
            .*\.md$|
            LICENSE$|
            .*\.txt$
          )$
      - id: trailing-whitespace
        exclude: |
          (?x)^(
            .*\.md$|
            .*\.txt$
          )$
      - id: check-merge-conflict
      - id: detect-private-key
        exclude: |
          (?x)^(
            tests/.*|
            .*\.example$
          )$
      - id: mixed-line-ending
        args: [--fix=lf]  # Standardize on LF line endings
        exclude: |
          (?x)^(
            .*\.bat$|
            .*\.ps1$|
            .*\.cmd$
          )$
      - id: debug-statements  # Check for leftover print/debugger statements
        exclude: |
          (?x)^(
            format\.py$|
            debug_.*\.py$|
            dev_utils\.py$|
            run\.py$
          )$
      - id: check-docstring-first
        exclude: |
          (?x)^(
            alembic/.*|
            tests/.*
          )$
      - id: check-executables-have-shebangs
        exclude: |
          (?x)^(
            .*\.py$
          )$
      - id: check-shebang-scripts-are-executable
        exclude: |
          (?x)^(
            .*\.py$
          )$

# Use system Python (works across platforms)
default_language_version:
  python: python

# Don't run on commit if CI is set (for automated commits)
ci:
  skip: [trailing-whitespace, end-of-file-fixer, mixed-line-ending]
  autofix_prs: false
  autoupdate_schedule: monthly
